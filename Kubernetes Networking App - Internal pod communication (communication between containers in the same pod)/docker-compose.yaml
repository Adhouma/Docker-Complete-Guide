version: "3"
services:
  auth:
    build: ./auth-api
  users:
    build: ./users-api
    # AUTH_ADDRESS refer to auth-api container (or auth service)
    environment:
      - AUTH_ADDRESS=auth
    ports: 
      - "8080:8080"
